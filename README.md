此仓库存放 Halo 商业版的电商页面模板，目前没有在 halo-pro 仓库中添加构建流程，所以暂时单独存放。

开发完成之后，需要：

1. 将 `templates/shop` 文件夹复制到 halo-pro 的 `application/src/main/resources/templates`
2. 将 `ec-dist` 文件夹复制到 halo-pro 的 `application/src/main/resources/static`
